import ResetPasswordForm from '@/components/shared/reset-password/ResetPasswordForm';

export const metadata = {
  title: 'Reset Password - Banana Korean',
  description: 'Create a new password for your Banana Korean account',
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
